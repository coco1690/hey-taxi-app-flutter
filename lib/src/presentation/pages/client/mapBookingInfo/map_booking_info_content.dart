import 'package:flutter/material.dart';

class MapBookingInfoContent extends StatelessWidget {
  
  const MapBookingInfoContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}